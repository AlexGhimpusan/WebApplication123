@model AspNetCoreEntityFrameworkApp.Models.Course

<h1>Course Details</h1>

<div>
    <h4>Course</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">ID</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Id)</dd>
        <dt class="col-sm-2">Name</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Name)</dd>
        <dt class="col-sm-2">Study Year</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.StudyYear)</dd>
        <dt class="col-sm-2">Announcements</dt>
        <dd class="col-sm-10">@Html.DisplayFor(model => model.Announcements)</dd>
    </dl>
</div>

<p>
    <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</p>
